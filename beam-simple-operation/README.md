# Simple Operation Tutorial
Apache Beam simple operation on Pipeline tutorial.

## Command to Execute
```bash
java -jar <jar_name> \
    --project=<project_id> \
    --stagingLocation=gs://<bucket>/staging \
    --tempLocation=gs://<bucket>/temp
```
